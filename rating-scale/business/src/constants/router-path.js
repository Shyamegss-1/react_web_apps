const BASE = "/rating-business";

const HOME = BASE;
const SIGNIN = BASE + "/signin";
const SIGNUP = BASE + "/signup";
const DASHBOARD = BASE + "/dashboard";

const REVIEWS = DASHBOARD + "/review";
const SETTING = DASHBOARD + "/setting";
const PROFILE = DASHBOARD + "/profile";
const CONTACT = DASHBOARD + "/contact";
const SUBSCRIPTION = DASHBOARD + "/pricing";

export const routes = {
  HOME,
  SIGNIN,
  SIGNUP,
  REVIEWS,
  SETTING,
  DASHBOARD,
  PROFILE,
  CONTACT,
  SUBSCRIPTION,
  BASE,
};
