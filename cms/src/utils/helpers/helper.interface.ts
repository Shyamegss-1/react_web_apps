export interface ifHeight {
  (id: string, value: number | string, side?: string): void;
}

export interface ifStyle {
  (id: string, prop: string): void;
}

export interface elSpacing {
  (id: string, value: number | string, side: string): void;
}
